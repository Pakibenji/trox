<h1 style="text-align :center; padding:20px">Modifier l'emprunt de : <%= @tool.title %></h1>
  <div class="d-flex justify-content-center">
    <%= form_with model: @loan, url: loan_path(@loan), local: true, method: :patch do |f| %>
      <div class="text-center">
      <%= f.label :start_date, "Date de début :", style:"padding: 20px" %><br>
      <%= f.date_field :start_date, style:"padding: 20px"%>
      </div>
      <div class="text-center">
      <%= f.label :end_date, "Date de fin :", style:"padding: 20px" %><br>
      <%= f.date_field :end_date, style:"padding: 20px" %>
      </div>
      <div class="d-flex justify-content-center" style="text-align :center; padding:30px">
        <%= f.submit "Envoyer la demande"%>
      </div>
    <% end %>
  </div>

